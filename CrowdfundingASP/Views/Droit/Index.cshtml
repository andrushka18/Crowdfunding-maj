@model IEnumerable<CrowdfundingASP.Models.Droit>

@{
    ViewBag.Title = "Index";
}

<h2>Role</h2>

<p>
    @Html.ActionLink("Ajouter", "Ajouter")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdDroit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Role)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdDroit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>
                @Html.ActionLink("Modifier", "Edit", new { id = item.IdDroit }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdDroit }) |
                @Html.ActionLink("Supprimer", "Delete", new { id = item.IdDroit })
            </td>
        </tr>
    }

</table>
<hr />
<div>
    @Html.ActionLink("Retour", "Index", "Home")
</div>
