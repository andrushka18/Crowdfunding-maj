@model IEnumerable<CrowdfundingASP.Models.Palier>

@{
    ViewBag.Title = "Index";
}

<h2>Palier</h2>

<p>
    @Html.ActionLink("Ajouter", "Ajouter")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdPalier)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Montant)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdPalier)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Montant)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prime)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Supprimer", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
<hr />
<div>
    @Html.ActionLink("Retour", "Index", "Home")
</div>
